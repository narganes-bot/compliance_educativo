"use strict";
/* Este archivo ya NO contiene la lógica: es un puente hacia la fuente única
 * de verdad en shared/engine/engine.js, compartida con la web (App.jsx).
 * No añadas lógica aquí — cualquier cambio en preguntas, riesgos o el
 * cálculo de probabilidad/impacto se hace en shared/engine/engine.js. */
module.exports = require("../../../shared/engine/engine.js");
